# features/

Feature-based modules (one folder per domain feature): `auth`, `curriculum`,
`lessons`, `progress`, `gamification`, `quizzes`, `helpdesk`, `labs`,
`interview`, `analytics`, `certificates`, `ai`.

Each feature owns its components, hooks, and logic. Shared primitives live in
`src/shared`. Populated phase by phase per docs/ARCHITECTURE.md.
