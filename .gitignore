node_modules/
/node_modules
.vite/
dist/

.env
.env.local

# Supabase CLI local state (project ref, pooler URL — not secrets, but
# machine-local and noisy in diffs).
supabase/.temp/

# Local agent/session tooling state — not part of the app.
.omc/
